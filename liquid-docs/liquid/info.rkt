#lang info

(define scribblings 
  '(
     ("liquid.scrbl" (multi-page) )
;;; couldn't figut out how to link these ingto the liquid.scrbl
;;; racket itself does not use scribble to accopmlish this on the top level doc
;;     ("concepts.scrbl" (multi-page) )
;;     ("reference.scrbl" (multi-page))
     ))


