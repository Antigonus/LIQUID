#lang racket

(require "sequence-lib-helpers.rkt")
;;(require (for-syntax "sequence-lib-helpers.rkt"))

(provide flatten-1)

(define (f n) n)





;;(define-syntax (X stx)
;;  (datum->syntax stx '(display (f 3)))
;;  )

